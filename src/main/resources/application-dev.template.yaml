spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/modura
    username: YOUR_USERNAME
    password: YOUR_PASSWORD

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

server:
  port: 8080